老师，你为啥不用多视图或者路由嵌套的方法公用头部
路由参数——刷新后希望数据还在

--------------------------------------------------------------------------------

假设：项目质量要求非常高

function getGoods(id){
  if(!id){
    throw 'no id';
  }else if(typeof id!='number'){
    throw 'id is not number';
  }

  let row=xxx;

  if(row...)
}

function getGoods(id:number){
  let row:goodsData=xxx;

}

--------------------------------------------------------------------------------

ts=>
1.属性初始化
2.constructor
  Vue：构建数据、router
3.created
  Vue：构建子元素、DOM
4.mounted

--------------------------------------------------------------------------------

push——向history末尾添加一级

replace——把history的最后一级直接改掉

--------------------------------------------------------------------------------

let arr=[1,2,3];

let result=[
  ...arr,
  5,6,7
]



let json={a: 12};
let res={
  ...json
}

--------------------------------------------------------------------------------

params参数：必填（可以是可选的，不太建议用很多）
query参数：可选

--------------------------------------------------------------------------------
