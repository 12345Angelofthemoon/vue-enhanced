//user——字符串
//password——字符串
//cb——function (err, data)

type callback=(a:boolean,b?:object)=>void;



function login(user:string, password:string, cb:callback){

}

login('blue', '123456', function (err:boolean){

});



// (a:number,b:number):number=>a+b;
// (number,number)=>number
