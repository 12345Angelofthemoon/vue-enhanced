1.Component、Watch、Prop、Vue
  @Component({
    props:
  })

  @Prop({name: string, password: string})

2.类型声明
  全局、局部

--------------------------------------------------------------------------------

客户端存储数据：
1.cookie      小、麻烦
2.xxxStorage
  sessionStorage        关闭浏览器消失
  localStorage          关闭浏览器还在

--------------------------------------------------------------------------------

两个电脑同时登录，把另一个账号挤下去怎么做的。。。
第二次登录的时候，服务器通知第一个客户端——退出
  websoket



如何做到“第二个人把第一个踢了”？
第二个人登录，就会踢掉第一个

多个客户端共同存在？
  token[]

--------------------------------------------------------------------------------


第一个人已经登录，第二个人登录不上去
核心：获得用户的在线状态
1.系统完成——websocket、长连接
2.每隔30s向服务器发个包（心跳），超过一定时间没再接到了——死了

--------------------------------------------------------------------------------
